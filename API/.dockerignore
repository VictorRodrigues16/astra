# Correção da V3: NÃO levar segredos nem lixo para a imagem.
node_modules
npm-debug.log

# segredos / ambiente — NUNCA dentro da imagem
.env
.env.*

# artefatos de seguranca e docs (não precisam na imagem)
security
*.md

# git / metadados
.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml
